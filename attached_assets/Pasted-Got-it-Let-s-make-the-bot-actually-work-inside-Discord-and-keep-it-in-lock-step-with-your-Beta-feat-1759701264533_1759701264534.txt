Got it. Let’s make the bot actually work inside Discord and keep it in lock-step with your Beta features. Below is a complete, end-to-end blueprint for THE COMMISH’s Discord bot + APIs + Supabase + DeepSeek + Sleeper. It’s opinionated, production-grade, and maps every Beta feature to concrete bot abilities, routes, tables, and tests—so you and Replit can wire it once and stop bleeding time.

I split this into 12 tight sections you can hand to the Replit Agent as a single work order.

⸻

0) North-Star: What “Working” Means

In a real server, after setup:
	•	/rules answers from your indexed constitution using DeepSeek.
	•	/digest posts today’s digest into the configured channel.
	•	/poll creates a poll with options + duration + tallies live.
	•	/whoami tells a user their mapped fantasy team (from Owner Mapping).
	•	Reminders (lineup lock, waivers, trades) post on schedule.
	•	Highlights / Rivalries post on triggers (weekly win, rivalry week).
	•	Bot reacts (👍❤️) to messages that meet your sentiment criteria (selective).
	•	All commands and posts honor channel routing, rate limits, idempotency, and produce traceable logs with request IDs.

⸻

1) Discord App & Permissions (must match code)

Scopes: bot applications.commands
Bot Intents:
	•	Privileged: Message Content (for sentiment/reactions)
	•	Recommended: Server Members (for mapping/help text)
Permissions integer (set on Bot tab and used in install URL):
	•	View Channels, Send Messages, Manage Messages, Read Message History, Embed Links, Add Reactions, Mention Everyone, Use Slash Commands, Create Polls.

Keep this integer in DISCORD_BOT_PERMISSIONS. The install URL must not have redirect_uri.

Install URL (generated by backend; what we fixed):
	•	client_id = your app id
	•	scope = bot applications.commands
	•	permissions = DISCORD_BOT_PERMISSIONS
	•	guild_id = selected guild
	•	disable_guild_select = true

Slash command registration: global (or per-guild during dev). Re-register on deploys.

⸻

2) Runtime Architecture (reliable by design)
	•	Express API /api/v2/* (session secured)
	•	Discord interaction endpoint /api/discord/interactions (raw body, Ed25519 verify)
	•	Worker (same process or separate):
	•	Job queue for outbound Discord posts (idempotent key = guildId:channelId:type:hash(payload); 3 retries; respect retry_after)
	•	Schedules (cron) for reminders/digests
	•	DeepSeek client with conservative timeouts + circuit breaker
	•	Sleeper client for league data
	•	Supabase for state (schema below)
	•	Feature flags & channel routing resolved per leagueId at execution time
	•	Observability: request IDs; structured logs; doctor endpoints

⸻

3) Supabase Schema (tables you need)

-- Core entities
CREATE TABLE IF NOT EXISTS accounts (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  email text UNIQUE NOT NULL,
  name text,
  plan text NOT NULL DEFAULT 'beta',
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS leagues (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  account_id uuid REFERENCES accounts(id),
  name text,
  features jsonb NOT NULL DEFAULT '{}'::jsonb,      -- { activated: true, demo: false, sleeper:{...}, ... }
  channels jsonb NOT NULL DEFAULT '{}'::jsonb,      -- { discord:{ guildId, channelId, pollsId?, highlightsId? } }
  personality jsonb NOT NULL DEFAULT '{}'::jsonb,   -- tone knobs for LLM
  created_at timestamptz NOT NULL DEFAULT now()
);

-- Discord user ↔ fantasy team mapping
CREATE TABLE IF NOT EXISTS owners (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  league_id uuid REFERENCES leagues(id) ON DELETE CASCADE,
  discord_user_id text,
  sleeper_owner_id text,
  display_name text,
  UNIQUE (league_id, discord_user_id),
  UNIQUE (league_id, sleeper_owner_id)
);

-- Indexed rules documents (for RAG)
CREATE TABLE IF NOT EXISTS rules_docs (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  league_id uuid REFERENCES leagues(id) ON DELETE CASCADE,
  title text,
  source text,                -- 'paste' | 'upload' | 'url'
  chunk_count int,
  created_at timestamptz DEFAULT now()
);

-- Scheduled reminders (presets + custom)
CREATE TABLE IF NOT EXISTS reminders (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  league_id uuid REFERENCES leagues(id) ON DELETE CASCADE,
  type text NOT NULL,         -- 'lineup_lock' | 'waivers' | 'trade_deadline' | 'custom'
  cron text NOT NULL,         -- cron or iso schedule
  channel_key text NOT NULL,  -- 'discord.main' or 'discord.polls'
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  enabled boolean NOT NULL DEFAULT true,
  last_run_at timestamptz
);

-- Bot activity & audit
CREATE TABLE IF NOT EXISTS bot_activity (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  league_id uuid,
  guild_id text,
  channel_id text,
  kind text,                  -- 'MESSAGE_POSTED' | 'COMMAND_RUN' | 'RETRY' | 'ERROR'
  key text,                   -- idempotency key
  status text,                -- 'ok' | 'retry' | 'fail'
  detail jsonb,
  created_at timestamptz DEFAULT now()
);


⸻

4) API Contracts (stable, predictable)

Setup
	•	GET /api/v2/discord/auth-url → { url }
	•	GET /api/v2/setup/discord-session → { guilds:[{id,name,icon}] }
	•	GET /api/v2/discord/channels?guildId=...
	•	200 { channels:[{id,name,type}] }
	•	409 { error:'BOT_NOT_IN_GUILD' }
	•	POST /api/v2/discord/bot-install-url?guildId=... (or GET) → { url }
	•	POST /api/v2/setup/account { email, name } → { ok, accountId }
	•	POST /api/v2/setup/discord { guildId, channelId, leagueId? } → { ok, leagueId }
	•	POST /api/v2/setup/sleeper { username, season, leagueId } → { ok }
	•	POST /api/v2/setup/activate { leagueId } → { ok }

Commissioner controls (Dashboard)
	•	GET /api/v2/leagues/:id → league record
	•	PATCH /api/v2/leagues/:id → update features/channels/personality
	•	GET /api/v2/owners?leagueId=...
	•	POST /api/v2/owners/map { leagueId, discordUserId, sleeperOwnerId }
	•	GET /api/v2/reminders?leagueId=...
	•	POST /api/v2/reminders / PATCH /api/v2/reminders/:id / DELETE ...

RAG
	•	POST /api/v2/rules/index { leagueId, title, text }
	•	GET /api/v2/rules/stats?leagueId=... → { docs, chunks }
	•	POST /api/v2/rules/ask { leagueId, question } → { answer, sources }

Doctors
	•	GET /api/_debug/health (admin) → DB/Discord/DeepSeek/session summary
	•	GET /api/v2/doctor/discord → bot user, base URL checks
	•	GET /api/v2/doctor/sleeper → ok
	•	GET /api/v2/doctor/ai → DeepSeek ok

⸻

5) Slash Commands (bot feature parity with Beta)

Command	Purpose	Options	Response
/help	Show command list and dashboard link	–	Ephemeral embed
/whoami	Show user’s mapped team	–	Ephemeral text
/rules	Ask constitution	question (string)	Ephemeral answer (RAG)
/scoring	Ask scoring rules	question (string)	Ephemeral
/poll	Create a poll	question, option1..5, duration	Public poll embed + tallies
/digest	Post daily/weekly digest	scope (`today	week), dry_run` (bool)
/reindex	Reindex rules	–	Ephemeral
/set-league	Set league for this channel (escape hatch)	league_id	Ephemeral
/doctor	Quick health summary in-guild	–	Ephemeral

Command resolution order:
	1.	From channel context (leagues.channels.discord)
	2.	From selectedLeagueId stored per guild/channel in leagues.channels
	3.	Fallback to /set-league or error if ambiguous

⸻

6) Events & Reactions (sentiment hygiene)

Events (Gateway Intents on):
	•	MESSAGE_CREATE (read content)
	•	GUILD_CREATE (cache guild/permissions)
	•	Optional MESSAGE_UPDATE (if you want to re-evaluate)

Selective reactions:
	•	Compute sentiment / criteria (e.g., regex on “GG”, “great trade”, or LLM classify); apply cooldowns per user/thread; react with 👍/❤️ only if not spammy and if bot has Add Reactions in that channel.

Permission guard:
	•	Before reacting or posting: verify VIEW_CHANNEL, READ_MESSAGE_HISTORY, SEND_MESSAGES, EMBED_LINKS, ADD_REACTIONS for the bot. If missing, log + ephemeral warn to commissioner.

⸻

7) DeepSeek & Sleeper flows (that actually return)

Rules Q&A (RAG → DeepSeek):
	1.	Retrieve top chunks from rules_docs vectors
	2.	Build prompt with league personality knobs (tone)
	3.	Call DeepSeek (short completion, streaming optional)
	4.	Return succinct answer + sources
	5.	If fail: graceful degradation with FAQ fallback

Digest:
	1.	Pull Sleeper (matchups, standings, transactions)
	2.	Summarize via DeepSeek with deterministic “bulletized” template
	3.	Post embed in channels.discord.channelId
	4.	Log bot_activity

Poll:
	•	Use Discord Polls or a custom message with reactions; in both cases, store a poll_id in bot_activity.detail for tally edits; close after duration.

Reminders:
	•	Cron per row in reminders → produce messages (respect channel route keys).
	•	Idempotency key = leagueId:reminderId:datebucket.

⸻

8) Reliability & Rate-limit discipline
	•	Idempotency: compute hash of normalized payload; drop dupes within 24h window.
	•	429: read Discord’s retry_after, exponential backoff (1s/2s/4s).
	•	Network: 2 retry budget on 5xx; never retry on 4xx (except 429).
	•	Poison pill: 3 fails → mark job failed, emit bot_activity with detail.
	•	Outbox pattern (optional): persist queued posts in DB, worker drains.
	•	Posting budget: per guild queue to avoid bursts.

⸻

9) Security
	•	/api/discord/interactions: express.raw, Ed25519 verify request signatures.
	•	Sessions: trust proxy, secure cookie, sameSite:lax, credentials:'include'.
	•	CSRF: apply to app POST routes, not the Discord signature endpoint.
	•	Admin endpoints: require X-Admin-Key.
	•	Minimal scopes: user OAuth uses identify guilds; bot auth uses bot applications.commands.
	•	Sanitize any user text echoed into embeds.

⸻

10) Observability
	•	Request IDs on every API + command (log [reqId]).
	•	bot_activity row per command/post/retry/error.
	•	/api/_debug/health: DB latency, Discord bot user id, DeepSeek model ping, session keys.
	•	/api/v2/doctor/*`: quick green/red for Discord, Sleeper, AI.
	•	Playwright artifacts: screenshots + console logs on failure.

⸻

11) Tests (agentic e2e you can re-run)

Playwright suites (extend what you have):
	•	00_doctor.spec.ts → doctors green
	•	01_home_cta.spec.ts → ok
	•	02_demo_activation.spec.ts → ok
	•	03_setup_account.spec.ts → ok
	•	04_setup_discord_install_gap.spec.ts → install flow works
	•	05_setup_discord_submit.spec.ts → no UUID crash; returns leagueId
	•	06_setup_sleeper.spec.ts → links league, writes into features.sleeper
	•	07_rules_index.spec.ts → stats > 0
	•	08_dashboard_beta.spec.ts → Beta badge, buttons wired (no 404s)
	•	09_discord_post_pipeline.spec.ts (admin) → post test, see idempotency/429 handling in logs
	•	10_commands_live.spec.ts (manual server with bot installed):
	•	Run /whoami, /rules question: "trade veto" (ephemeral returns), /poll (public), /digest dry_run:true (ephemeral)
	•	Assert Discord API calls succeed (via API spy logs) and bot_activity rows created

Sweep script (one button):
	•	Hit doctors
	•	Hit route manifest
	•	Verify Supabase migrations exist & columns present (channels jsonb, features jsonb, defaults)
	•	Dump current league row and ensure features.activated === true after setup
	•	Send a test post; check it lands and no duplicates are posted when re-run

⸻

12) Rollout order for Replit (do this, in order)
	1.	DB: run gen_random_uuid defaults + ensure channels, features, personality exist (jsonb).
	2.	Server:
	•	Install URL endpoint (no redirect_uri)
	•	Channels endpoint returns BOT_NOT_IN_GUILD
	•	POST /api/v2/setup/discord accepts optional leagueId, generates on server
	•	Doctors: discord, sleeper, ai; admin health
	•	Interaction endpoint raw + signature verify
	•	Job queue wrapper for posting (idempotent + retries)
	3.	Client:
	•	Onboarding: Account → Discord (install flow with pending-guild-id) → Sleeper → Rules → redirect to /app, store selectedLeagueId
	•	AppShell: compute Demo vs Beta by featureFlags.demo
	•	Dashboard: real actions wired; it’s fine if display stats are mock temporarily
	4.	Secrets: set DISCORD_BOT_PERMISSIONS, verify APP_BASE_URL lowercase.
	5.	Doctors: run them; fix anything red before clicking through.
	6.	Playwright: run full suite; save artifacts.
	7.	Live validation: use /doctor & /help inside Discord, then /rules, /poll, /digest.

⸻

Frequent gotchas (so you don’t stall):
	•	Empty UUID crash (you hit this): always generate leagueId server-side if missing.
	•	Install URL using redirect_uri: remove it (user OAuth uses redirect; bot install does not).
	•	Bot not in guild: backend must return BOT_NOT_IN_GUILD; frontend must show Install and auto-retry after return.
	•	Session cookie not sticking: trust proxy, secure cookie, credentials:'include'.
	•	Interactions route broken by body parser: raw body before any JSON middlewares.
	•	429 spam: respect retry_after, idempotent keys, queue per guild.
	•	Commands not showing: re-register slash commands after changing definitions.

⸻

Where this maps to your Beta features
	•	Commissioner Dashboard toggles → stored in leagues.features & channels; bot reads these at runtime for every command/post.
	•	Channel routing → leagues.channels.discord.{ mainId, pollsId, highlightsId } used per feature.
	•	Bot personality → leagues.personality injected into DeepSeek prompts.
	•	Owner mapping → /whoami and digest mentions resolve to team names.
	•	Reminders & schedules → reminders table + cron worker; posts are queued.
	•	RAG quality → /rules/index + /rules/ask with DeepSeek; /reindex when docs change.
	•	Engagement → /poll, selective reactions, highlights/rivalries as scheduled/triggered posts.
	•	Reliability → posting pipeline wrapper (idempotency, retries, rate limit).
	•	Security → cookies set right, CSRF on app endpoints, signatures on interactions.
	•	Observability → doctors + bot_activity.

⸻

If you hand this exact blueprint to Replit and have them implement the missing server endpoints, the posting wrapper, and the Discord interaction handlers per the contracts above, your bot will not just install—it will function and stay aligned with the Beta feature set you see in the dashboard.

If you want, I can turn this into a single “Replit Work Order.md” plus a migration script and a minimal command handler skeleton next.